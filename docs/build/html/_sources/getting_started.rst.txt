Getting Started
===============

You can install SyncToolbox using the Python package manager pip:

.. code-block:: bash

    pip install synctoolbox

Or using conda:

.. code-block:: bash

    conda env create -f environment.yml
    conda activate synctoolbox
